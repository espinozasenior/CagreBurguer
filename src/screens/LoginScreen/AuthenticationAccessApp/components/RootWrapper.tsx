import styled from "styled-components";

export const FormAccessWrapper = styled.div`
    margin: -100px auto 0;
    width:80%;
`

const RootWrapper = styled.div`
    display:flex;
    flex-direction:column;
`

export default RootWrapper;